# 📁 Data Directory

⚠️ **هذا المجلد لا يحتوي على بيانات حقيقية في Git**

## البنية:
```
data/
├── .gitkeep              ← موجود في Git
├── README.md             ← موجود في Git
├── users.json            ← يُنشأ محلياً (غير موجود في Git)
├── projects.json         ← يُنشأ محلياً (غير موجود في Git)
└── invoices.json         ← يُنشأ محلياً (غير موجود في Git)
```

## التشغيل أول مرة:
```bash
npm run init
```

هذا الأمر ينشئ:
- ملفات JSON فارغة
- مستخدمين تجريبيين
- بيانات أولية للاختبار

## البيانات التجريبية:

راجع `examples/sample-data.json` لمثال على البنية.
